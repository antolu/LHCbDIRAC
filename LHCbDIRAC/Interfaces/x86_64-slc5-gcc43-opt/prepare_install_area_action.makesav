#-- start of make_header -----------------

#====================================
#  Document prepare_install_area_action
#
#   Generated Tue Apr 10 10:55:41 2012  by phicharp
#
#====================================

include ${CMTROOT}/src/Makefile.core

ifdef tag
CMTEXTRATAGS = $(tag)
else
tag       = $(CMTCONFIG)
endif

cmt_prepare_install_area_action_has_target_tag = 1

#--------------------------------------------------------

ifdef cmt_prepare_install_area_action_has_target_tag

tags      = $(tag),$(CMTEXTRATAGS),target_prepare_install_area_action

Interfaces_tag = $(tag)

#cmt_local_tagfile_prepare_install_area_action = $(Interfaces_tag)_prepare_install_area_action.make
cmt_local_tagfile_prepare_install_area_action = $(bin)$(Interfaces_tag)_prepare_install_area_action.make

else

tags      = $(tag),$(CMTEXTRATAGS)

Interfaces_tag = $(tag)

#cmt_local_tagfile_prepare_install_area_action = $(Interfaces_tag).make
cmt_local_tagfile_prepare_install_area_action = $(bin)$(Interfaces_tag).make

endif

include $(cmt_local_tagfile_prepare_install_area_action)
#-include $(cmt_local_tagfile_prepare_install_area_action)

ifdef cmt_prepare_install_area_action_has_target_tag

cmt_final_setup_prepare_install_area_action = $(bin)setup_prepare_install_area_action.make
#cmt_final_setup_prepare_install_area_action = $(bin)Interfaces_prepare_install_area_actionsetup.make
cmt_local_prepare_install_area_action_makefile = $(bin)prepare_install_area_action.make

else

cmt_final_setup_prepare_install_area_action = $(bin)setup.make
#cmt_final_setup_prepare_install_area_action = $(bin)Interfacessetup.make
cmt_local_prepare_install_area_action_makefile = $(bin)prepare_install_area_action.make

endif

cmt_final_setup = $(bin)setup.make
#cmt_final_setup = $(bin)Interfacessetup.make

#prepare_install_area_action :: ;

dirs ::
	@if test ! -r requirements ; then echo "No requirements file" ; fi; \
	  if test ! -d $(bin) ; then $(mkdir) -p $(bin) ; fi

javadirs ::
	@if test ! -d $(javabin) ; then $(mkdir) -p $(javabin) ; fi

srcdirs ::
	@if test ! -d $(src) ; then $(mkdir) -p $(src) ; fi

help ::
	$(echo) 'prepare_install_area_action'

binobj = 
ifdef STRUCTURED_OUTPUT
binobj = prepare_install_area_action/
#prepare_install_area_action::
#	@if test ! -d $(bin)$(binobj) ; then $(mkdir) -p $(bin)$(binobj) ; fi
#	$(echo) "STRUCTURED_OUTPUT="$(bin)$(binobj)
endif

ifdef use_requirements
$(use_requirements) : ;
endif

#-- end of make_header ------------------
#-- start of cmt_action_runner_header ---------------

ifdef ONCE
prepare_install_area_action_once = 1
endif

ifdef prepare_install_area_action_once

prepare_install_area_actionactionstamp = $(bin)prepare_install_area_action.actionstamp
#prepare_install_area_actionactionstamp = prepare_install_area_action.actionstamp

prepare_install_area_action :: $(prepare_install_area_actionactionstamp)
	$(echo) "prepare_install_area_action ok"
#	@echo prepare_install_area_action ok

$(prepare_install_area_actionactionstamp) :: $(prepare_install_area_action_dependencies)
	$(silent) /home/phicharp/cmtuser/Dirac_v99/DiracPolicy/scripts/prepare_install_area_links /home/phicharp/cmtuser/LHCbDirac_v99/InstallArea
	$(silent) cat /dev/null > $(prepare_install_area_actionactionstamp)
#	@echo ok > $(prepare_install_area_actionactionstamp)

prepare_install_area_actionclean ::
	$(cleanup_silent) /bin/rm -f $(prepare_install_area_actionactionstamp)

else

prepare_install_area_action :: $(prepare_install_area_action_dependencies)
	$(silent) /home/phicharp/cmtuser/Dirac_v99/DiracPolicy/scripts/prepare_install_area_links /home/phicharp/cmtuser/LHCbDirac_v99/InstallArea

endif

install ::
uninstall ::

#-- end of cmt_action_runner_header -----------------
#-- start of cleanup_header --------------

clean :: prepare_install_area_actionclean ;
#	@cd .

ifndef PEDANTIC
.DEFAULT::
	$(echo) "(prepare_install_area_action.make) $@: No rule for such target" >&2
#	@echo "#CMT> Warning: $@: No rule for such target" >&2; exit
	if echo $@ | grep '$(package)setup\.make$$' >/dev/null; then\
	 echo "$(CMTMSGPREFIX)" "(prepare_install_area_action.make): $@: File no longer generated" >&2; exit 0; fi
else
.DEFAULT::
	$(echo) "(prepare_install_area_action.make) PEDANTIC: $@: No rule for such target" >&2
	if echo $@ | grep '$(package)setup\.make$$' >/dev/null; then\
	 echo "$(CMTMSGPREFIX)" "(prepare_install_area_action.make): $@: File no longer generated" >&2; exit 0;\
	 elif test $@ = "$(cmt_final_setup)" -o\
	 $@ = "$(cmt_final_setup_prepare_install_area_action)" ; then\
	 found=n; for s in 1 2 3 4 5; do\
	 sleep $$s; test ! -f $@ || { found=y; break; }\
	 done; if test $$found = n; then\
	 test -z "$(cmtmsg)" ||\
	 echo "$(CMTMSGPREFIX)" "(prepare_install_area_action.make) PEDANTIC: $@: Seems to be missing. Ignore it for now" >&2; exit 0 ; fi;\
	 elif test `expr $@ : '.*/'` -ne 0 ; then\
	 test -z "$(cmtmsg)" ||\
	 echo "$(CMTMSGPREFIX)" "(prepare_install_area_action.make) PEDANTIC: $@: Seems to be a missing file. Please check" >&2; exit 2 ; \
	 else\
	 test -z "$(cmtmsg)" ||\
	 echo "$(CMTMSGPREFIX)" "(prepare_install_area_action.make) PEDANTIC: $@: Seems to be a fake target due to some pattern. Just ignore it" >&2 ; exit 0; fi
endif

prepare_install_area_actionclean ::
#-- end of cleanup_header ---------------
